inputplus
=========
The function is to add/remove input boxes and assign the values to a hidden input box inside a form.

Usage:
new addinputbox({
  holder_id: holder_id,
  hidden_id: hidden_id,
  seprator: ";"
})
